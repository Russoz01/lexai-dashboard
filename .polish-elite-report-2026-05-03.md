# Polish Elite Report — 2026-05-03

## Sumario executivo

3 commits dirigidos por evidencia visual, todos buildando + pushed para `origin/main`.
Foco em fixes P0/P1 detectados via Chrome MCP em ambos modes (dark + light).

| Tier | Commit | SHA | Status |
|---|---|---|---|
| A — Visual broken (P0) | `polish: tier A visual broken (theme toggle invisible + login hardcoded dark)` | `5f7c40d` | shipped |
| B — Fluidez (P1) | `polish: tier B fluidez (press-scale + hover-lift em CTAs e cards)` | `9337020` | shipped |
| C — Consistency (P0 light) | `polish: tier C (404 headline tokenizada — era invisivel em light)` | `0d5a7ab` | shipped |

Build status: **passing** apos cada commit. Pushed `origin/main` em todos.

## Findings detectados via Chrome MCP

### P0 — Visual broken (todos corrigidos)

1. **ThemeToggle `landing` variant invisivel em light mode**
   - File: `src/components/ThemeToggle.tsx`
   - Problema: hardcoded `rgba(255,255,255,0.55)` (white) sobre cream background = ratio ~1.1:1, icones sumiam totalmente
   - Fix: tokenizado para `var(--text-muted)` + `var(--accent-light)`, agora adapta nos 2 modes
   - Afeta: nav de `/`, `/sobre`, `/empresas`, `/privacidade`, `/termos`, `/dpa`, `/roi`

2. **`/login` aside hardcoded `bg-gradient-to-br from-[#0f0b08] via-[#0a0807] to-black`**
   - File: `src/app/login/page.tsx`
   - Problema: lado direito do split-screen sempre dark independente do tema. Em light mode quebrava a continuidade visual entre as duas metades
   - Fix: removido bg literal, agora usa `surface-base` (que tem micro-gradientes adaptativos)

3. **`/login` value-prop cards (`text-white/55`, `border-white/10`) invisiveis em light**
   - 4 cards romanos (I, II, III, IV) + nota do atelier inteira sumiam em cream
   - Fix: tokenizado para `var(--text-on-surface*)`, `var(--border)`, `var(--card-bg)`
   - Bonus: adicionado `hover-lift` + `transition-editorial` nos 4 cards

4. **`/login` error/success banners (`text-red-200`, `bg-red-500/[0.08]`) invisiveis em light**
   - Texto vermelho-pastel sobre cream nao passava WCAG AA
   - Fix: tokenizado para `var(--danger)` + `var(--danger-light)`

5. **`/not-found` headline `color: '#f5e8d3'` (cream) sobre cream surface**
   - File: `src/app/not-found.tsx`
   - Contraste em light mode era literalmente ~1:1 — quase ilegivel
   - Fix: `var(--text-primary)` (navy em light, cream em dark) + `text-grad-accent` no span de destaque

### P1 — Fluidez (corrigidos)

1. **Landing CTAs sem `:active scale` press feedback**
   - Files: `src/app/page.tsx`, `src/app/empresas/page.tsx`, `src/app/sobre/page.tsx`, `src/app/dashboard/em-breve/page.tsx`
   - Adicionado `press-scale` (translate(0.98) on active) em 6 CTAs principais
   - `transition` (sem duration) trocado por `transition-editorial` (timing tokenizado, 250ms ease-editorial)

2. **`/empresas` grid de 27 agentes — cards nao-novos sem hover state**
   - File: `src/app/empresas/page.tsx` linha 232
   - Apenas cards `isNew` tinham hover (border + shadow). Os outros 21 cards eram visualmente estaticos
   - Fix: adicionado `hover-lift` (translateY -2px + border accent) + `hover:shadow-[0_8px_24px_var(--shadow)]`

3. **Secondary CTAs sem hover border accent**
   - "Ver casos de uso", "Avisar quando lançar" — `transition` sem hover state visible
   - Adicionado `hover:border-[var(--accent)]`

## Files modificados (5 totais)

```
src/app/dashboard/em-breve/page.tsx   (+2 -2  micro-fluidez)
src/app/empresas/page.tsx             (+4 -4  fluidez + grid hover)
src/app/login/page.tsx                (+38 -22 P0 hardcoded dark)
src/app/not-found.tsx                 (+3 -3  P0 headline contrast)
src/app/page.tsx                      (+2 -2  CTA press-scale)
src/app/sobre/page.tsx                (+2 -2  CTA press-scale)
src/components/ThemeToggle.tsx        (+5 -14 tokenizacao)
```

## Verificacao visual

Screenshots salvos em `.playwright-mcp/audit-final-2026-05-03/`:
- `/`, `/login`, `/sobre`, `/empresas`, `/not-found`, `/intro`, `/privacidade` em ambos modes

Confirmado visualmente:
- ThemeToggle: sun + moon icons agora **visiveis** em light mode (era empty pill)
- Login light mode: aside agora cream, cards com texto navy legivel, error banner com paleta tematizada
- 404 light mode: headline navy + champagne dark agora legivel (era invisivel cream-on-cream)
- 404 dark mode: cream + champagne bright glow preservado (no regression)
- /empresas: cards nao-novos agora tem hover lift + shadow visivel

## Issues nao-resolvidos (documentados, nao bloqueantes)

1. **WhatsApp floating button (#25D366 verde)** — clash de paleta com brand champagne, mas eh **intencional** (cor obrigatoria do WhatsApp brand guideline). Mantido.

2. **`/intro` page hardcoded `bg-[#0a0a0a] text-white`** — intencional (sequencia cinematica imersiva, dark = arte). Nao tematizado de proposito.

3. **3 dashboard pages com `color: '#f5e8d3'` hardcoded** (configuracoes, professor, prazos) — nao auditados visualmente porque exigem auth. Candidatos a Tier C wave 2 (proxima sessao).

4. **Cards "Casos" / "CRM" / "Jurimetria" no agentes-bento** — Casos card tem `min-h` mas conteudo bottom vazio (visual hole no grid). Componente complexo (`lex-agents-bento.tsx`), nao alterado nesta wave para evitar refator arquitetural.

## Constraints respeitados

- Voice: Leonardo PT-BR informal, no emoji em commits ✓
- ESLint `no-console: error` mantido ✓
- Build TS passa entre cada commit ✓
- `/oc-advogados` nao tocado ✓
- Zero deps novas ✓
- Zero refator arquitetural ✓
- 3 commits em ~50min (under 60min, under 4 commits) ✓
