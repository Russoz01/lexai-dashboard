import type { Metadata } from 'next'
import { DM_Sans, Playfair_Display } from 'next/font/google'
import { ThemeProvider } from '@/context/ThemeContext'
import { CookieConsent } from '@/components/CookieConsent'
import './globals.css'

const dmSans = DM_Sans({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-dm-sans',
  display: 'swap',
})

const playfair = Playfair_Display({
  subsets: ['latin'],
  weight: ['600', '700', '800'],
  variable: '--font-playfair',
  display: 'swap',
})

export const metadata: Metadata = {
  metadataBase: new URL('https://lexai.com.br'),
  title: 'LexAI — Inteligencia Juridica · uma marca Vanix Corp',
  description: 'LexAI, uma marca Vanix Corp. Plataforma de inteligencia artificial para escritorios de advocacia. 12 agentes IA especializados: analise de documentos, pesquisa jurisprudencial, redacao de pecas, negociacao, calculos juridicos e mais.',
  keywords: 'lexai, vanix corp, assistente juridico, inteligencia artificial, direito, advogado, escritorio advocacia, jurisprudencia, pecas processuais, ia juridica',
  authors: [{ name: 'Vanix Corp' }],
  openGraph: {
    title: 'LexAI — Assistente Juridico Inteligente',
    description: '12 agentes de IA para profissionais do Direito. Analise documentos, pesquise jurisprudencia e gere pecas processuais.',
    type: 'website',
    locale: 'pt_BR',
    url: 'https://lexai.com.br',
    siteName: 'LexAI · Vanix Corp',
    // Images auto-generated by src/app/opengraph-image.tsx
  },
  twitter: {
    card: 'summary_large_image',
    title: 'LexAI',
    description: '12 agentes de IA para escritorios de advocacia. Analise documentos, pesquise jurisprudencia e gere pecas processuais.',
    // Images auto-generated by src/app/opengraph-image.tsx
  },
  alternates: {
    canonical: 'https://lexai.com.br',
  },
  robots: { index: true, follow: true },
}

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'LexAI',
  applicationCategory: 'BusinessApplication',
  operatingSystem: 'Web',
  description: 'Plataforma de inteligencia artificial para advogados brasileiros. 12 agentes IA especializados em analise de documentos, pesquisa jurisprudencial, redacao de pecas, calculos juridicos e mais.',
  offers: [
    { '@type': 'Offer', name: 'Escritorio', price: '1399', priceCurrency: 'BRL', billingIncrement: 'P1M', description: '1-5 advogados · 5 agentes' },
    { '@type': 'Offer', name: 'Firma', price: '1459', priceCurrency: 'BRL', billingIncrement: 'P1M', description: '6-15 advogados · 10 agentes' },
    { '@type': 'Offer', name: 'Enterprise', price: '1599', priceCurrency: 'BRL', billingIncrement: 'P1M', description: '16+ advogados · agentes customizados' },
  ],
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <head>
        <script dangerouslySetInnerHTML={{ __html: `
          (function() {
            try {
              var t = localStorage.getItem('lexai-theme');
              if (!t) t = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
              document.documentElement.setAttribute('data-theme', t);
            } catch(e) {
              document.documentElement.setAttribute('data-theme', 'dark');
            }
          })();
        `}} />
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
        />
      </head>
      <body className={`${dmSans.variable} ${playfair.variable}`}>
        <ThemeProvider>{children}</ThemeProvider>
        <CookieConsent />
      </body>
    </html>
  )
}
