() => { function parseColor(s){if(!s)return null;s=s.trim();if(s==='transparent'||s==='none')return null;if(s.startsWith('#')){let h=s.slice(1);if(h.length===3)h=h.split('').map(c=>c+c).join('');if(h.length!==6)return null;return{r:parseInt(h.slice(0,2),16),g:parseInt(h.slice(2,4),16),b:parseInt(h.slice(4,6),16),a:1}}const m=s.match(/rgba?\(([^)]+)\)/);if(!m)return null;const p=m[1].split(/[,\s/]+/).filter(Boolean);const r=parseFloat(p[0]),g=parseFloat(p[1]),b=parseFloat(p[2]);const a=p.length>=4?parseFloat(p[3]):1;if([r,g,b].some(v=>isNaN(v)))return null;return{r,g,b,a}} function blend(f,b){if(!f)return null;if(f.a>=.999)return{...f,a:1};if(!b)return f;const a=f.a+b.a*(1-f.a);return{r:(f.r*f.a+b.r*b.a*(1-f.a))/a,g:(f.g*f.a+b.g*b.a*(1-f.a))/a,b:(f.b*f.a+b.b*b.a*(1-f.a))/a,a}} function rl({r,g,b}){const f=c=>{c=c/255;return c<=.03928?c/12.92:Math.pow((c+.055)/1.055,2.4)};return .2126*f(r)+.7152*f(g)+.0722*f(b)} function ct(f,b){const a=rl(f),c=rl(b);return(Math.max(a,c)+.05)/(Math.min(a,c)+.05)} function rb(el){let c=el;while(c&&c!==document.documentElement){const cs=getComputedStyle(c);const b=parseColor(cs.backgroundColor);if(b&&b.a>.05){if(b.a<.999){let p=c.parentElement;while(p&&p!==document.documentElement){const pcs=getComputedStyle(p);const pb=parseColor(pcs.backgroundColor);if(pb&&pb.a>.05){if(pb.a>=.999)return{bg:blend(b,pb),source:c.tagName};p=p.parentElement;continue}p=p.parentElement}const bb=parseColor(getComputedStyle(document.documentElement).getPropertyValue('--bg-base').trim())||{r:255,g:255,b:255,a:1};return{bg:blend(b,bb),source:c.tagName+'+body'}}return{bg:b,source:c.tagName}}c=c.parentElement}const bb=parseColor(getComputedStyle(document.documentElement).getPropertyValue('--bg-base').trim())||{r:245,g:239,b:230,a:1};return{bg:bb,source:'body'}} function vis(el){const r=el.getBoundingClientRect();if(r.width<4||r.height<4)return false;if(r.bottom<-200)return false;const cs=getComputedStyle(el);if(cs.visibility==='hidden'||cs.display==='none')return false;if(parseFloat(cs.opacity)<.1)return false;return true} function tx(el){let t='';for(const n of el.childNodes)if(n.nodeType===3)t+=n.nodeValue;return t.trim().slice(0,60)} function dz(el){const i=el.id?'#'+el.id:'';const c=el.className&&typeof el.className==='string'?'.'+el.className.split(' ').filter(Boolean).slice(0,3).join('.'):'';return el.tagName.toLowerCase()+i+c} function lg(cs){const fs=parseFloat(cs.fontSize);const fw=parseInt(cs.fontWeight,10)||400;return fs>=24||(fs>=18.66&&fw>=700)} const iss=[];const seen=new Set(); document.querySelectorAll('h1,h2,h3,h4,h5,h6,p,a,button,span,label,li,td,th,small').forEach(el=>{ if(!vis(el))return;const t=tx(el);if(!t||t.length<2)return; const cs=getComputedStyle(el);const fg=parseColor(cs.color);if(!fg||fg.a<.05)return; const bi=rb(el);const fgB=blend(fg,bi.bg);if(!fgB)return; const r=ct(fgB,bi.bg);const lar=lg(cs);const th=lar?3:4.5; if(r<th){const sig=dz(el)+'|'+t.slice(0,40);if(seen.has(sig))return;seen.add(sig); iss.push({el:dz(el),text:t,fs:parseFloat(cs.fontSize).toFixed(0),fw:cs.fontWeight,color:cs.color,bg:`rgb(${bi.bg.r.toFixed(0)},${bi.bg.g.toFixed(0)},${bi.bg.b.toFixed(0)})`,ratio:parseFloat(r.toFixed(2)),thr:th,large:lar,bgSrc:bi.source,sev:r<2?'P0':(r<(th-0.5)?'P1':'P2')});} }); return{count:iss.length,issues:iss,theme:document.documentElement.getAttribute('data-theme'),url:location.pathname,bgBase:getComputedStyle(document.documentElement).getPropertyValue('--bg-base').trim()};}
